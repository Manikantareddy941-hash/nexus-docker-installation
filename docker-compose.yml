## docker-compose.yml (optional but smarter)
```yaml
version: "3.8"

services:
  nexus:
    image: sonatype/nexus3
    container_name: nexus
    ports:
      - "8081:8081"
    volumes:
      - nexus-data:/nexus-data
    restart: unless-stopped

volumes:
  nexus-data:
Run with:

bash
Copy code
docker compose up -d
